services:
  postgres:
    image: postgres:16
    container_name: pg-aibackend
    environment:
      POSTGRES_DB: aibackend
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
